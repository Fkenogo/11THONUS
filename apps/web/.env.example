# Placeholder keys only — no real values. Copy to .env.local and fill in per environment.
# These names were captured from the retired temporary Firebase CLI scaffold
# (.env.local at the repo root); actual Firebase project wiring is Phase 1 work
# (ENG-P1-xxx) once DEC-TECH-005 (region) is resolved.
VITE_BACKEND_PROVIDER=
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=
